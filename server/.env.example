DATABASE_URL=mongodb+srv://<user>:<password>@<cluster>.mongodb.net
DATABASE_NAME=your_db_name
PORT=3001

# Optional: set true only on machines where Node reports "unable to verify the first certificate"
# MONGODB_TLS_ALLOW_INVALID_CERTS=true

JWT_SECRET=change_me
